---
apiVersion: v1
kind: Service
metadata:
  name: kometa-quickstart
  namespace: apps
  labels: { app: kometa-quickstart }
spec:
  type: ClusterIP
  selector: { app: kometa-quickstart }
  ports:
  - { name: http, port: 7171, targetPort: 7171 }
