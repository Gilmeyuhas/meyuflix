---
apiVersion: v1
kind: Service
metadata:
  name: kometa
  namespace: apps
  labels: { app: kometa }
spec:
  type: ClusterIP
  selector: { app: kometa }
  ports:
  - { name: http, port: 7171, targetPort: 7171 }
